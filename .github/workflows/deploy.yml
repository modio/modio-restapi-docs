name: deploy

on:
  push:
    branches:
      - 'public'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run deployment script
        shell: bash
        run: |
          chmod +x ./deploy_gh.sh
          gem install bundler
          ./deploy_gh.sh